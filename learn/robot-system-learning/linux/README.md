本Readme.md文件存放每日学习笔记，项目实践内容等
# day1学习记录
/是系统根目录
home 存放用户个人文件
etc 系统配置文件目录，系统设置中心
var 变化数据目录
usr 用户程序目录（系统安装的程序目录）
opt 安装额外的软件
tmp 临时数据目录
dev 设备文件目录（硬件设备）
| 目录      | 作用    | 机器人开发例子          |
| ------- | ----- | ---------------- |
| `/`     | 根目录   | 整个系统             |
| `/home` | 用户文件  | 代码、工程            |
| `/etc`  | 系统配置  | 网络、服务、启动         |
| `/var`  | 变化数据  | 日志、缓存            |
| `/usr`  | 系统软件  | python、git、ros工具 |
| `/opt`  | 第三方软件 | CUDA、ROS         |
| `/tmp`  | 临时文件  | 编译缓存             |
| `/dev`  | 设备文件  | 串口、摄像头、磁盘        |

练习linux基础操作指令
cd ls cat mkdir ./文件运行 cp touch 


# day2学习记录
完成了 Linux 基础命令练习：
ls -a / ls -h / ls -lh
mkdir / touch / cp / mv / cat / head / tail / grep / wc
完成了 Shell 脚本练习：
day2_demo.sh
day2_demo_if_for.sh
以及执行了脚本输出

# Day3 Linux、Shell 与 Git 学习记录

## 1. 学习目标

- 理解路径、重定向和常用文本处理命令。
- 练习 Shell 变量、参数、数组、条件判断和循环。
- 练习 Git 分支、工作区恢复和 stash。

## 2. 已完成的操作

### Linux

- 使用 `pwd`、`pwd -P`、`ls -ld .` 和 `ls -ld ..` 查看路径与目录信息。
- 使用 `mkdir`、`touch`、`cp`、`mv` 和 `rm` 操作文件与目录。
- 使用 `>` 覆盖文件，使用 `>>` 追加内容。
- 分别将正常输出和错误输出重定向到文件。
- 练习了 `grep`、`sort`、`uniq` 和 `sed`。

### Shell

- 创建并运行了 `day3_args.sh`。
- 练习了普通变量、环境变量和数组。
- 使用 `if/elif/else` 完成条件判断。
- 使用 `for`、`while` 和 `until` 完成循环。

### Git

- 创建并切换过 `day3-practice` 分支。
- 在练习分支上创建过提交。
- 使用了 `git restore`、`git stash` 和 `git stash pop`。

## 3. 重点理解

### 路径

- 绝对路径从 `/` 开始，不依赖当前目录。
- 相对路径以当前目录为起点。
- `.` 表示当前目录，`..` 表示上一级目录。
- `pwd` 默认显示 Shell 维护的逻辑路径；`pwd -P` 会解析符号链接并显示物理路径。路径中没有符号链接时，两者结果可能相同。

### 重定向

- 标准输出的文件描述符是 `1`，用于正常结果。
- 标准错误的文件描述符是 `2`，用于错误和诊断信息。
- `>` 或 `1>` 将标准输出覆盖写入文件。
- `>>` 将标准输出追加到文件末尾。
- `2>` 将标准错误写入文件。
- `2>&1` 让标准错误使用标准输出当前指向的位置。
- `ls > list.txt 2> err.txt` 执行成功时，`err.txt` 为空是正常现象，因为命令没有产生标准错误。
- 重定向可以保存程序运行日志，方便后续检查和定位问题。

### 文本处理

- `grep` 根据条件筛选行，`grep -n` 还会显示行号。
- `sort` 对文本行进行排序，不负责筛选内容。
- `uniq` 合并相邻的重复行，通常与 `sort` 组合使用：`sort file | uniq`。
- `sed -n '1,5p' file` 显示文件第 1 到第 5 行。
- `grep 't*'` 中的 `*` 是正则量词，表示前面的 `t` 出现零次或多次，因此它可能匹配不含 `t` 的行。查找具体文本可使用 `grep 'third' file`。

### Shell 变量和参数

- 普通 Shell 变量默认只属于当前 Shell。
- 使用 `export` 导出的环境变量可以被当前 Shell 启动的子进程继承。
- `$HOME` 保存当前用户的主目录。
- `$PATH` 保存命令搜索目录，多个目录使用 `:` 分隔。
- 变量可以写成 `$name` 或 `${name}`；为避免空格和通配符造成拆分，通常使用 `"$name"`。
- `$0` 是脚本名称，`$1`、`$2` 是第一、第二个位置参数，`"$@"` 表示全部位置参数。
- 执行 `./day3_args.sh robot linux` 时，`robot` 对应 `$1`，`linux` 对应 `$2`。
- `-gt`、`-lt`、`-eq` 分别表示整数大于、小于和等于。
- `while` 在条件成立时继续循环；`until` 在条件成立时结束循环。

### Git 工作区域

- 工作区是正在编辑的文件。
- 暂存区保存下一次提交准备包含的版本，由 `git add` 更新。
- `git commit` 将暂存区内容保存为本地仓库中的提交。
- `git push` 只是把本地提交同步到远程仓库，不是进入提交区的条件。

## 4. 遇到的问题与纠正

### `git restore` 恢复什么

- 默认的 `git restore file` 使用暂存区版本覆盖工作区文件。
- 已被 Git 跟踪的文件不需要在每次恢复前重新执行 `git add`。
- 如果先修改文件并执行 `git add`，随后又继续修改，那么 `git restore file` 会恢复到刚刚暂存的版本。
- 未跟踪文件没有 Git 保存的基准版本，因此普通的 `git restore` 无法恢复它。
- `git restore` 会丢弃工作区修改，执行前应先查看 `git diff`，避免误删有效内容。

### `git stash` 的“保存现场”和“后续恢复”

- “保存现场”是把尚未提交的已跟踪文件修改暂存到 stash，使工作区暂时恢复干净，便于切换任务或分支。
- `git stash list` 用于查看已经保存的现场。
- `git stash pop` 会重新应用最近的现场，并在成功后删除对应的 stash 记录。
- 普通 `git stash` 默认不包含未跟踪文件；需要同时保存未跟踪文件时使用 `git stash -u`。
- 本次练习中的 `temp.txt` 在 stash 后仍显示为未跟踪文件，说明它没有被普通 `git stash` 保存；实际保存的是当时已被 Git 跟踪的文件修改。

## 5. Day3 总结

- 已能完成基础文件操作、输出重定向和简单文本处理。
- 已能编写带参数的 Shell 脚本，并使用数组、判断和循环。
- 已实际操作 Git 分支、restore 和 stash。
- 当前最容易混淆的是 Git 的工作区、暂存区、本地提交、远程仓库，以及 stash 是否包含未跟踪文件。
- 这些知识可用于保存机器人程序日志、编写自动化脚本，以及安全管理项目代码变更。

## 6. 后续复习重点

- 使用多行且包含重复内容的文件再次验证 `sort`、`uniq` 和 `grep` 的区别。
- 再练一次 `git restore`，分别观察工作区、暂存区和 `HEAD` 中的版本。
- 对比 `git stash` 与 `git stash -u` 对未跟踪文件的处理结果。

# Day4 Linux 系统信息与诊断脚本学习记录

## 1. 学习目标

- 使用 `file`、`stat` 查看文件类型和元数据。
- 使用 `uname`、`hostname`、`df`、`du`、`free`、`uptime` 查看系统状态。
- 理解命令退出状态、函数返回状态和脚本退出状态。
- 编写并验证 `day4_system_check.sh` 系统诊断脚本。
- 同时验证正常流程和缺少依赖命令的异常流程。

## 2. 实际执行与结果

### 文件和系统信息

- `file day3_args.sh` 判断该文件是 Bash 脚本，并显示它包含 UTF-8 编码的 Unicode 文本。
- `stat day3_args.sh` 查看文件大小、权限、所有者、时间、硬链接数和 inode。
- `uname -r` 输出内核版本 `6.8.0-136-generic`。
- `uname -m` 输出架构 `x86_64`。
- `hostname` 输出主机名 `test`。
- `/etc/os-release` 显示当前发行版是 Ubuntu 22.04.5 LTS。

### 磁盘、内存和负载

- `df -h` 查看各文件系统的总容量、已用空间和可用空间。
- `du -sh robot-system-learning` 显示学习目录占用约 `260K`。
- `free -h` 显示总内存、已用内存、空闲内存、缓存和可用内存。
- `uptime` 显示当前时间、开机时长、登录用户数，以及过去 1、5、15 分钟的平均负载。

### 退出状态

- `true` 不需要输出文字，但执行成功，因此退出状态为 `0`。
- `false` 不需要输出文字，但用于返回失败，因此退出状态为 `1`。
- `command -v uname` 能找到命令，返回 `0`。
- `command -v day4_missing_command` 找不到命令，返回非零状态。
- Shell 直接执行不存在的命令时通常返回 `127`，表示 command not found。

### 诊断脚本验证

- 默认输出文件运行成功，六个依赖命令均显示 `[OK]`，脚本返回 `0`。
- 自定义日志文件 `day4_custom.log` 运行成功，脚本返回 `0`。
- 两份正常日志均包含时间、主机名、内核、架构、根文件系统、内存和平均负载。
- 使用 `DAY4_TEST_MISSING=1` 注入不存在的命令后，脚本输出 `[ERROR]` 并返回 `1`。
- 异常流程没有生成 `day4_failed.log`，证明脚本在写报告前已经停止。

## 3. 重点理解

### 文件类型、元数据和 inode

- `file` 主要回答“这个文件的内容属于什么类型”。
- `stat` 主要回答“这个文件在文件系统中具有什么属性”。
- 文件名是目录项中的名称，目录项把文件名关联到 inode。
- inode 保存文件的核心元数据，同一个 inode 可以通过多个硬链接文件名访问。
- `Unicode text, UTF-8 text` 表示文本包含 Unicode 字符并使用 UTF-8 编码，不表示它是二进制程序。

### 内核、发行版和架构

- Linux 严格来说是内核。
- Ubuntu 是基于 Linux 内核构建的完整发行版，包含系统工具、软件包管理、服务和可选桌面环境，不只是操作界面。
- `x86_64` 主机为 `aarch64` 目标构建程序时，通常需要对应的交叉编译工具链。

### 磁盘与内存

- `df` 的统计对象是文件系统，适合查看某个挂载点还剩多少空间。
- `du` 的统计对象是指定文件或目录，适合查看它实际占用多少磁盘空间。
- `free` 表示当前完全空闲的内存。
- `available` 还考虑系统可回收的缓存，更适合估计在不发生明显交换时还能提供给程序的内存。

### 平均负载

- `uptime` 的三个负载值依次对应过去 1、5、15 分钟。
- 负载统计处于可运行状态或不可中断睡眠状态的平均任务数，不是 CPU 百分比。
- 负载需要结合逻辑 CPU 数判断。例如负载 `4.0` 在 4 核系统上可能接近满载，在 16 核系统上通常仍有较多余量。

### Shell 状态和参数

- `$?` 只保存上一条命令的退出状态；执行新命令后会立即被覆盖，所以必须及时检查。
- `echo hello` 是有效的 Shell 内置命令，成功后会把 `$?` 改为 `0`。
- `${1:-day4_system_check.log}` 表示：第一个参数未设置或为空时使用默认日志名，否则使用第一个参数。
- `local command_name` 将变量限制在当前函数内，避免影响函数外的同名变量。
- `return 0` 或 `return 1` 结束当前函数，并向调用者返回成功或失败状态。
- `exit 1` 结束整个脚本，而不只是结束当前函数。
- `check_command "$command_name" || exit 1` 表示只有命令检查失败时，才以状态 `1` 终止脚本。
- `>&2` 把错误文字写入标准错误，便于将正常结果和错误信息分开处理。

## 4. 诊断脚本的执行流程

`day4_system_check.sh` 是一个最小系统诊断工具，流程如下：

1. 根据第一个参数决定日志名称；没有参数时使用默认名称。
2. 检查 `date`、`uname`、`hostname`、`df`、`free`、`uptime` 六个依赖命令。
3. 如果缺少依赖，向标准错误输出原因并以状态 `1` 结束，不生成不完整报告。
4. 如果依赖完整，收集主机、内核、架构、磁盘、内存和负载信息。
5. 将系统信息写入日志，并告诉用户日志保存位置。

## 5. 遇到的问题、根因与解决

### 脚本没有执行权限

- 现象：运行脚本时提示“权限不够”。
- 根因：脚本最初没有执行权限。
- 解决：使用 `chmod +x day4_system_check.sh` 添加执行权限。

### 相对路径使用错误

- 现象：在 `robot-system-learning/linux` 目录中再次使用 `robot-system-learning/linux/day4_system_check.sh`，提示文件不存在。
- 根因：相对路径从当前目录开始计算，重复写入了目录层级。
- 解决：已经位于脚本目录时直接使用 `./day4_system_check.sh`。

### 命令输入错误

- `at day4_custom.log` 应为 `cat day4_custom.log`。
- `;s` 不是有效命令，分号出现在没有前置命令的位置会产生语法错误。

### `echosss` 导致日志内出现错误

- 现象：日志中出现 `echosss: 未找到命令`，但脚本仍可能返回 `0`。
- 根因：`echosss` 在 Shell 语法上可以作为命令名，因此 `bash -n` 不会报语法错误；运行时才会发现命令不存在。后续的 `uptime` 成功又让命令组最终返回成功状态。
- 解决：将 `echosss` 修正为 `echo`，重新执行正常和异常验证。
- 经验：语法检查通过不等于运行逻辑正确，还必须检查日志、退出状态和产出文件。

## 6. Day4 学习总结

- 已学会查看文件类型、文件属性、系统架构、磁盘、内存和平均负载。
- 已理解正常命令、失败命令、函数和整个脚本之间的退出状态传递。
- 已完成一个能够检查依赖并保存系统信息的诊断脚本。
- 已通过真实错误理解：测试不能只看屏幕输出，还要同时验证退出状态和文件结果。
- 该脚本可作为后续机器人系统诊断工具的基础，继续加入 ROS2、网络、CAN 和设备状态检查。

## 7. 后续复习重点

- 再练习一次 `${变量:-默认值}`，区分“未传参数”和“传入自定义参数”。
- 继续区分 Shell 的 `return` 与 `exit`，避免把函数状态和程序退出混为一谈。
- 学习如何让诊断脚本在报告生成过程中任一命令失败时可靠返回非零状态。

# Day5 高级文件筛选与日志搜索学习记录

## 1. 学习目标

- 使用多个 `find` 条件筛选日志文件。
- 理解文件搜索与内容搜索是两个不同阶段。
- 在受控临时目录中练习先预览、后删除。
- 使用 `day5_find_logs.sh` 区分匹配成功、没有匹配、目录不存在和没有日志文件。

## 2. 实际执行与结果

### 文件筛选

- 在 `robot-system-learning/linux` 中找到了两份 560 字节的 Day4 日志。
- 使用 `-iname` 成功以不区分大小写的方式找到 `day4_custom.log`。
- 使用文件大小和修改时间条件完成了组合筛选。
- 将大小条件改为大于 1000 字节后没有输出，正确判断为无匹配，而不是命令错误。

### 内容搜索

- 搜索 `Kernel` 时，两份日志都找到了匹配行。
- 搜索 `DAY5_NOT_FOUND` 时没有匹配，实际退出状态为 `1`。
- 已理解文件遍历状态不能在所有场景下准确表达内容是否匹配，因此项目脚本直接保存 `grep` 的退出状态。

### 安全删除

- 删除范围严格限制在 `/tmp/day5-delete-practice`。
- 删除前先预览，只显示 `remove.log`。
- 删除后 `remove.log` 不再存在，`keep.txt` 得到保留。

### 脚本验证

`day5_find_logs.sh` 的五个场景均符合预期：

- 找到 `Kernel`：返回 `0`。
- 日志存在但关键字不存在：返回 `1`。
- 搜索目录不存在：返回 `2`。
- 目录存在但没有日志：返回 `3`。
- 搜索以 `-` 开头的文本：不会被当作选项，无匹配时返回 `1`。

## 3. 脚本整体逻辑

1. 读取搜索目录和关键字；没有参数时使用默认值。
2. 先判断搜索目录是否存在。
3. 使用 `find` 收集日志路径，并安全地保存到 Bash 数组。
4. 没有日志文件时直接返回状态 `3`。
5. 使用 `grep` 搜索全部日志并立即保存其退出状态。
6. 根据状态输出匹配成功、没有匹配或执行错误。

这里的重点是理解流程，不要求记忆 `mapfile`、`-print0` 等参数细节。使用空字符分隔路径并保存到数组，是为了避免文件名中的空格或换行被错误拆分。

## 4. 学习问题与解答

### 为什么 `find -exec grep` 的状态不能简单等同于匹配结果

- 使用 GNU `find -exec ... {} +` 时，如果找到了文件并执行 `grep`，`grep` 的非零结果可能使 `find` 最终也返回非零。
- 如果没有找到任何文件，`grep` 根本没有执行，而 `find` 仍可能返回 `0`。
- 所以只查看 `find` 的状态，无法可靠区分“找到关键字”和“没有文件可搜索”。脚本直接运行 `grep` 并保存它的状态，含义更明确。

### 为什么使用 `-print0` 和数组保存路径

- 普通空格或换行分隔可能把一个文件名拆成多个部分。
- 空字符不能出现在 Linux 文件名中，适合作为路径之间的可靠分隔符。
- 数组让每个日志路径作为一个独立参数传给 `grep`。

### `grep` 和脚本退出状态表示什么

- `grep` 返回 `0` 表示找到匹配，返回 `1` 表示没有匹配，大于 `1` 表示执行错误。
- 脚本返回 `0` 表示找到关键字，`1` 表示没有匹配行，`2` 表示目录不存在，`3` 表示没有日志文件。

### 为什么关键字前使用 `--`

- `--` 表示选项到此结束。
- 后面的 `-n` 等内容会被当作普通搜索文本，而不是命令选项。

### 为什么删除前必须使用相同条件预览

- 预览可以确认搜索起点、深度、文件类型和名称条件是否正确。
- 只有预览结果完全符合预期，才把最终动作从显示改为删除。
- `-delete` 不能撤销，因此不能省略预览步骤。

## 5. 遇到的问题与纠正

### `find -exec grep` 返回 `1`

- 现象：搜索不存在的关键字时，实际状态为 `1`，与“可能返回 0”的初始理解产生疑问。
- 原因：本次已经找到日志并执行了批量 `grep`，无匹配时 GNU `find` 反映了被执行命令的失败状态。
- 结论：不能把一种场景的 `find` 状态推广到所有场景；内容搜索工具应直接处理 `grep` 状态。

### 搜索没有输出

- 现象：筛选大于 1000 字节的日志时没有输出。
- 原因：现有日志均为 560 字节，没有文件同时满足全部条件。
- 结论：无输出不一定是错误，应结合筛选条件和退出状态判断。

## 6. Day5 学习总结

- 已学会按名称、类型、大小和修改时间组合筛选文件。
- 已建立“先限定范围、再预览、最后删除”的安全操作习惯。
- 已理解日志搜索脚本的整体流程和四种业务状态。
- 对部分 Shell 参数细节还不熟悉，但不影响理解脚本的用途、执行顺序和结果判断。

# Day6 grep 取证与日志等级分析学习记录

## 1. 学习目标

- 理解日志文件经过筛选后如何形成故障证据。
- 区分固定文本匹配与正则表达式匹配的效果。
- 查看错误行前后的上下文，辅助判断故障过程。
- 使用 `day6_log_analyzer.sh` 将日志归类为健康、警告或错误。
- 保持分析工具只读，避免修改原始日志证据。

## 2. 实际执行与结果

### 固定测试数据

- 在 `/tmp/day6-grep-practice` 中创建了 `clean.log`、`warning.log` 和 `error.log`。
- 三份日志分别代表正常、包含警告、同时包含警告和错误的场景。
- 测试数据位于 `/tmp`，不作为项目源码提交。

### 固定文本与正则匹配

- 普通 `grep` 搜索 `[ERROR]` 时显示了全部五行，产生误匹配。
- 使用固定文本匹配时只显示两行带完整 `[ERROR]` 标签的日志。
- 使用正则成功找到了温度数值，并同时筛选出一行警告和两行错误。

### 上下文取证

- 错误上下文显示 `motor timeout` 前存在温度警告，之后出现 `retry motor`。
- 上下文提供了故障前后的执行线索，但它只是证据，不能单独证明根因。

### 日志分析器验证

- 正常日志统计为零个错误、零个警告，显示 `HEALTHY`。
- 警告日志统计为一个警告，显示警告证据和 `WARN`。
- 错误日志统计为两个错误、一个警告；错误优先，显示错误证据和 `ERROR`。
- 输入文件不存在时，脚本明确提示日志文件不存在。
- 独立复验中三份输入日志的哈希值前后相同，证明脚本没有修改日志。

## 3. 脚本整体逻辑

`day6_log_analyzer.sh` 的处理流程如下：

1. 接收并检查输入日志文件。
2. 统计固定错误标签和警告标签。
3. 如果存在错误，优先输出错误证据和 `ERROR`。
4. 没有错误但存在警告时，输出警告证据和 `WARN`。
5. 两者都不存在时，输出 `HEALTHY`。

学习重点是理解这条数据流，不要求记忆脚本参数或数字状态码。

## 4. 学习问题与解答

### `grep` 在日志分析中的输入、处理和输出是什么

- 输入是一份日志文件或其他文本。
- 处理是根据指定条件筛选需要关注的行。
- 输出是匹配内容以及用于判断结果的证据。

### 为什么固定标签适合按字面量匹配

- `[ERROR]` 是一个完整、固定的日志标签。
- 普通正则会把方括号解释为特殊规则，只匹配其中任意一个字符，容易产生误匹配。
- 字面量匹配把整个标签当作普通文字，结果与需求一致。

### 正则表达式适合什么场景

- 当内容存在变化规律时适合使用正则，例如温度后面的数字。
- 当需要表达多个可能模式时也可使用正则，例如同时匹配 `ERROR` 或 `WARN`。
- 这部分只需知道用途，不要求用户记忆或独立编写正则。

### 上下文能提供什么证据

- 上下文显示故障前的状态和故障后的动作。
- 它能帮助建立事件顺序、缩小排查范围，但不能仅凭相邻几行确定根因。

### 为什么错误优先于警告

- 错误通常代表更严重的问题，需要优先处理。
- 同一日志同时包含错误和警告时，最终结果应先反映最高严重等级。
- 警告表示需要关注的异常或风险，不保证系统一定不受影响。

### 为什么分析脚本不修改日志

- 原始日志是定位和复现故障的证据。
- 分析过程中修改或删除日志会破坏证据，也会让后续复验失去可靠输入。
- 日志读取和日志清理应由不同工具负责。

## 5. 遇到的问题与纠正

### `[ERROR]` 出现误匹配

- 现象：原本只想找错误标签，普通搜索却显示全部五行。
- 原因：方括号在正则表达式中表示匹配其中任意一个字符。
- 解决：固定标签改用字面量匹配。
- 经验：匹配结果过多时，先检查搜索条件是否被当作正则解释。

### 对日志等级的理解过于绝对

- 原理解：警告不影响运行，错误表示系统一定无法执行。
- 纠正：警告表示存在需要关注的异常或风险；错误表示更严重的事件，但是否导致整个系统停止还需结合上下文判断。

## 6. Day6 完成结论

- 已完成固定文本、正则表达式和上下文取证实验。
- 已完成日志分析器的正常、警告、错误和无效输入验证。
- 已理解日志分析器从读取输入、统计等级到输出最高严重程度的整体流程。
- 按用户要求，正则细节、数字状态码和每日总结不作为后续验收内容。

# Day7 日志查看能力验证记录

## 1. 验证方式

- Day7 原主题是 `cat`、`less`、`head`、`tail`。
- `cat`、`head`、`tail` 的基础用途已在 Day2 学过，因此不重复完成整套命令和脚本实践。
- 本次通过五个日志查看场景问题验证已有能力。
- 验证重点是工具选择、首尾快照的证据边界、实时观察和原始日志保护，不考参数或状态码。

## 2. 能力验证结果

### 查看不同位置的日志

- 用户知道使用 `head` 查看文件开头。
- 用户知道使用文本搜索定位日志中间的错误。
- 用户知道 `tail -f` 可以持续显示服务后续写入的内容。
- 用户能区分普通 `tail` 的一次性快照和 `tail -f` 的持续观察。

### 大日志查看

- 用户理解 `cat` 会把整个文件输出到终端，大日志不便浏览和定位。
- 用户理解 `less` 更适合分页查看大文件。
- 搜索大日志时无需先使用 `cat` 建立管道，可以直接让搜索工具读取日志，减少不必要的命令环节。

### 证据边界

- 用户明确指出：开头和结尾正常，不能排除日志中间存在问题。
- 首尾快照只能代表被显示的部分，工具必须提示“中间内容未包含”。
- 查看上下文或完整搜索结果后，才能对日志中的故障情况作进一步判断。

### 原始日志保护

- 用户理解修改原始日志会影响开发人员的故障分析。
- 日志查看和快照工具应保持只读，避免破坏原始证据。
- 日志读取、归档和清理应由职责明确的不同流程负责。

## 3. 问题与纠正

### `cat file | grep ERROR` 是否必要

- 这种写法可以传递文本，但 `cat` 在这里没有提供额外价值。
- 更直接的思路是让搜索工具读取日志文件。
- 学习重点不是记住具体写法，而是减少无意义环节并保持处理流程清晰。

### “最新内容”与“持续新增内容”

- 只查看执行时的最新几行属于一次性尾部快照。
- 需要观察运行中服务后续写入的记录时，才使用持续跟踪方式。

## 4. Day7 完成结论

- 五项能力验证覆盖了 Day7 的核心学习目标。
- 用户已经具备选择日志查看方式、理解证据边界和保护原始日志的能力。
- `day7_log_snapshot.sh` 属于重复性练习，不再要求创建。
- Day7 记为“能力验证通过，跳过重复实践”。

# Day8 诊断管道与证据保存学习记录

## 1. 学习目标

- 理解管道中上游输出与下游输入的关系。
- 理解标准输出和标准错误是两条不同的数据通道。
- 使用 `tee` 同时显示筛选结果并保存报告。
- 避免旧报告污染本次测试结论。
- 使用逐段检查定位管道中首次偏离预期的位置。
- 完成 `day8_diagnostic_pipeline.sh` 诊断管道工具。

## 2. 实际执行与结果

### 筛选与统计管道

- 混合日志包含五行记录。
- 筛选阶段得到一条警告和两条错误。
- 筛选结果进入统计阶段后得到三行，而不是统计原日志全部五行。
- 已理解管道左侧是上游，左侧输出成为右侧下游的输入。

### 标准输出与标准错误

- `[NORMAL] diagnostic output` 是标准输出，经过管道进入 `tee`。
- `tee` 将该内容显示在终端并写入 `stdout-only.txt`。
- `[ERROR STREAM] simulated failure` 使用标准错误，默认绕过管道直接显示在终端。
- 因此屏幕显示了两行，但报告文件只包含标准输出的一行。

### 使用 tee 保存证据

- 混合日志经过筛选后，屏幕和 `attention-report.txt` 都得到三行关注信息。
- 使用正常日志重新运行后，报告被覆盖为零行，旧的三行没有残留。
- 这证明报告是可重新生成的分析产物，每次测试应反映本次输入，而不是历史结果。

### 诊断脚本验证

- 混合日志显示三条关注信息，报告包含三行，结论为 `attention required`。
- 正常日志覆盖同一报告后，报告为零行，结论为 `no warning or error found`。
- 输入日志不存在时，脚本明确提示输入文件不存在，不输出虚假的分析结论。
- 脚本语法检查和独立复验均通过。

## 3. 诊断管道整体逻辑

`day8_diagnostic_pipeline.sh` 的处理流程如下：

1. 接收输入日志与报告文件路径。
2. 检查输入日志是否存在。
3. 从日志中筛选警告和错误。
4. 使用 `tee` 把筛选结果显示在屏幕并覆盖写入本次报告。
5. 统计报告中的关注行数。
6. 根据是否存在关注行显示最终结论。

学习重点是理解数据如何逐阶段传递，不要求记忆命令参数或数字状态码。

## 4. 学习问题与解答

### 管道中的上游和下游是什么

- 管道左侧命令是上游，右侧命令是下游。
- 上游的标准输出成为下游的标准输入。
- 下游处理的是上游给出的内容，不一定是原始文件的全部内容。

### 为什么屏幕错误不一定进入报告

- 管道默认只连接标准输出。
- 标准错误默认绕过管道并直接显示在终端。
- 因此“屏幕看见了错误”和“报告保存了错误”是两件不同的事。

### tee 解决什么问题

- `tee` 接收上游送来的标准输入。
- 它把同一份内容写入报告，同时继续输出到屏幕或下一阶段。
- `tee` 不会自动收集没有进入管道的标准错误。

### 为什么不能保留旧报告冒充新结果

- 旧报告对应的是历史输入和历史执行过程。
- 如果本次没有覆盖生成报告，旧内容可能被误认为当前故障证据。
- 报告必须和本次输入及本次执行一一对应。

### 为什么分析报告可以覆盖，原始日志不能修改

- 报告是根据原始日志生成的派生结果，可以重新生成。
- 原始日志是定位和复现问题的证据，修改后会丢失真实现场。
- 分析工具必须保持输入只读。

## 5. 遇到的问题与纠正

### 标准输出和标准错误方向混淆

- 初始理解把 `[NORMAL]` 误认为标准错误。
- 文件内容提供了直接证据：`stdout-only.txt` 保存了 `[NORMAL]`，说明它经过了管道和 `tee`。
- 最终确认：没有 `>&2` 的正常文字是标准输出；带 `>&2` 的模拟错误是标准错误。

### 对 tee 的作用理解错误

- 初始理解认为 `tee` 可以解决标准错误无法进入报告的问题。
- 纠正后确认：`tee` 只复制已经到达其标准输入的数据，不会自动改变上游的错误通道。

### 空报告的逐段排查

- 原始混合日志确实包含异常。
- 单独执行筛选阶段得到三行，说明输入和筛选阶段正常。
- `script-report.txt` 为空，是因为它此前被正常日志覆盖，之后没有用混合日志重新生成。
- 结论：空报告不是当前混合日志的有效分析结果；问题位于报告生成时序，而不是原始日志或筛选条件。

## 6. Day8 完成结论

- 已完成管道数据流、输出通道边界、`tee` 保存证据和逐段排查练习。
- 已能依据文件内容纠正标准输出与标准错误的理解。
- 已理解报告必须对应本次执行，原始日志必须保持只读。
- Day8 所有核心学习目标已经通过验证。

# Day9 重定向能力复核记录

## 1. 原计划内容

- Day9 的原定主题是 Linux 重定向。
- 核心内容包括覆盖、追加、正常输出、错误输出以及诊断结果保存。

## 2. 已有学习证据

### Day3 重定向实践

- 已使用 `>` 覆盖文件，使用 `>>` 追加内容。
- 已将正常输出和错误输出分别写入文件。
- 已理解 `1>`、`2>` 和 `2>&1` 在输出分流与合并中的整体作用。

### Day4 项目实践

- `day4_system_check.sh` 已将系统检查结果保存为日志。
- 脚本使用重定向合并收集诊断输出，并验证了正常和异常路径。

### Day8 输出通道与证据保存

- 已通过实际文件内容确认标准输出与标准错误是两条不同通道。
- 已理解终端看到的错误不一定会进入管道或报告。
- 已使用 `tee` 同时显示和保存结果，并理解本次报告应覆盖旧结果。
- 已理解诊断工具可以覆盖派生报告，但必须保持原始输入只读。

## 3. Day9 结论

- Day9 要求的重定向知识和项目实践已在 Day3、Day4 和 Day8 完成并经过复核。
- 不再重复安排命令或新增 `day9` 脚本。
- Day9 记为“已有能力证据充足，归档后跳过”，按原学习计划进入 Day10。

# Day10 环境变量与 PATH 学习记录

## 1. 学习目标

- 理解普通 Shell 变量与环境变量对子进程的影响。
- 理解 `PATH` 在 Shell 命令查找中的作用。
- 通过故意改错 `PATH` 复现命令找不到，并恢复正常命令查找。
- 创建并加载 `robot_env.sh` 项目环境文件。

## 2. 实际执行与结果

### 普通变量与环境变量

- 当前 Shell 同时能读取普通变量 `DAY10_LOCAL` 和已导出的 `DAY10_ENV`。
- 子进程中 `DAY10_LOCAL` 为空，`DAY10_ENV` 仍为 `environment-value`。
- 根据输出确认：普通 Shell 变量不会自动传给子进程，使用 `export` 导出的环境变量可以被子进程继承。

### PATH 与命令查找

- 已将 `PATH` 拆分为多个搜索目录，并查看 `bash` 的实际位置。
- `/tmp/day10-bin/robot-health` 使用完整路径可直接运行。
- `/tmp/day10-bin` 未加入 `PATH` 时，Shell 无法只通过 `robot-health` 这个命令名找到它。
- 将该目录加入 `PATH` 后，Shell 找到 `/tmp/day10-bin/robot-health` 并成功运行。
- 已理解 `PATH` 保存搜索目录列表，Shell 按顺序在其中查找与命令同名的可执行文件。

### PATH 故障与恢复

- 将 `PATH` 临时改为只包含 `/tmp/day10-bin` 后，`robot-health` 仍可被找到。
- 此时外部 `ls` 程序无法通过 `PATH` 找到，终端明确提示 `/bin` 和 `/usr/bin` 不在 `PATH` 中。
- 恢复保存的 `PATH` 后，Shell 重新找到 `/usr/bin/ls`，并能正常执行。
- 已理解“命令找不到”不一定表示软件未安装，也可能是命令所在目录不在 `PATH` 中。

### robot_env.sh 项目实践

- `robot_env.sh` 语法检查通过。
- 文件定义并导出项目根目录 `ROBOT_PROJECT_ROOT` 和临时日志目录 `ROBOT_LOG_DIR`。
- 文件将项目的 Linux 脚本目录放在现有 `PATH` 前面，没有丢失原有系统命令目录。
- 加载后，Shell 能直接找到 `day8_diagnostic_pipeline.sh` 和 `day3_args.sh`。
- `/tmp/robot-system-logs` 日志目录创建成功，不作为 Git 提交内容。

## 3. 整体逻辑

### 环境变量解决什么问题

- 项目可以用统一的变量向后续启动的脚本和程序传递路径、模式或其他配置。
- 普通变量默认只属于当前 Shell，导出后才能作为环境的一部分传给子进程。

### PATH 解决什么问题

- 使用完整路径时，Shell 直接运行指定文件。
- 只输入命令名时，Shell 通过 `PATH` 找到程序。
- 更新 `PATH` 时必须保留原有内容，否则已安装的系统命令也可能无法通过命令名找到。

### robot_env.sh 的处理流程

1. 定义机器人项目根目录。
2. 定义项目临时日志目录。
3. 将项目 Linux 脚本目录加入现有 `PATH`。
4. 将这些配置导出，供当前 Shell 启动的后续程序继承。

## 4. 遇到的问题与纠正

### command -v 首先显示 ls 别名

- 现象：将 `PATH` 改错后，`command -v ls` 仍显示 `alias ls='ls --color=auto'`。
- 原因：`command -v` 显示 Shell 对该名称的解析结果，当前交互 Shell 中的 `ls` 首先是别名，这不能证明外部 `ls` 文件仍在当前 `PATH` 中。
- 检查：改为只查找外部命令文件，并实际执行外部 `ls`。
- 结果：错误 `PATH` 下查找失败且 `ls` 无法执行；恢复后找到 `/usr/bin/ls` 并正常运行。

### 是否需要修改 ~/.bashrc

- Day10 只要求在当前 Shell 中验证项目环境生效。
- 持久加载不纳入 Day10 完成标准，用户今天不需要掌握或修改 `~/.bashrc`。

## 5. Day10 完成结论

- 已通过实际输出确认普通变量与环境变量的继承差异。
- 已理解 `PATH` 的命令查找逻辑，并能复现和恢复 `PATH` 故障。
- 已完成 `robot_env.sh` 并验证项目环境生效。
- Day10 所有核心学习目标已通过检查。

# Day11 source 与当前 Shell 学习记录

## 1. 学习目标

- 理解 `source` 会在当前 Shell 中执行文件内容。
- 区分直接运行脚本与使用 `source` 加载脚本的结果。
- 理解变量和函数会留在哪个 Shell 中。
- 能根据路径、文件和语法证据检查加载失败。
- 从项目目录之外加载 `robot_env.sh` 并验证项目环境。

## 2. 实际执行与结果

### 临时配置文件

- 在 `/tmp/day11-source-practice` 中创建了 `day11_config.sh`。
- 文件定义环境变量 `DAY11_MODE` 和函数 `day11_status`。
- 文件内容正确，`bash -n` 语法检查通过。
- 该文件是 `/tmp` 中的临时练习产物，不提交到 Git。

### 直接运行与 source 加载

- 直接运行 `day11_config.sh` 后，当前 Shell 中 `DAY11_MODE` 仍为空，`day11_status` 函数不存在。
- 这证明脚本内的变量和函数只留在执行脚本的子 Shell，子 Shell 结束后不会回写当前 Shell。
- 使用 `source` 加载同一文件后，`DAY11_MODE` 为 `diagnostic`，Shell 能显示并调用 `day11_status` 函数。
- 函数运行结果为 `[DAY11] mode=diagnostic`，证明文件内容已在当前 Shell 中执行。

### 加载失败与排查

- 加载不存在的 `missing_config.sh` 时，Shell 明确提示文件不存在。
- 失败后当前 Shell 仍能显示检查文字，之前加载的 `day11_status` 也仍可使用。
- 已使用当前目录、文件路径和语法检查定位问题。
- 最终理解排查顺序：先看路径，再确认文件存在且可读，然后检查语法和加载结果。

### 项目环境加载

- 在 `/tmp` 目录中使用完整路径成功加载项目的 `robot_env.sh`。
- `ROBOT_PROJECT_ROOT` 和 `ROBOT_LOG_DIR` 显示正确值。
- Shell 能通过加载后的 `PATH` 找到 `day8_diagnostic_pipeline.sh`。
- 之前加载的 `day11_status` 函数仍可使用，证明两次 `source` 都修改了同一个当前 Shell。

### 清理临时内容

- 清理后 `DAY11_MODE` 显示为空。
- Shell 不再能找到 `day11_status` 函数。
- 已理解 `source` 的改动留在当前 Shell，需要时也应在当前 Shell 中清理。

## 3. 整体逻辑

### 直接运行

```text
当前 Shell → 启动子 Shell → 子 Shell 执行文件 → 子 Shell 结束
```

- 子 Shell 中新建的变量和函数不会回写给当前 Shell。

### source 加载

```text
当前 Shell → source 读取文件 → 文件内容在当前 Shell 执行
```

- 变量赋值、函数定义和其他 Shell 命令都可以直接改变当前 Shell。
- `export` 让变量还可以被子进程继承，但它不是变量和函数留在当前 Shell 的原因。

### robot_env.sh 加载流程

1. `source` 按指定路径读取 `robot_env.sh`。
2. 当前 Shell 执行文件中的变量赋值和 `export`。
3. 项目目录被放到现有 `PATH` 前面，原有搜索目录继续保留。
4. 后续命令可以立即使用项目变量，并通过 `PATH` 找到项目脚本。

## 4. 遇到的问题与纠正

### 引号未闭合

- 现象：初次输入 `cat "$practice_dir/day11_config.sh` 时漏写结尾引号，Shell 显示 `>` 继续提示符。
- 处理：使用 `Ctrl+C` 取消未完成命令，补齐引号后成功查看文件。
- 理解：继续提示符表示 Shell 还在等待命令完成，不是程序卡死。

### 直接运行后的变量为什么没有留下

- 初始回答误认为与 `PATH` 和 `unset` 有关。
- 纠正后确认：原因是直接运行脚本使用子 Shell，子 Shell 的改动不会回写当前 Shell；`unset` 只用于清理测试前的旧内容。

### source 与 export 的作用混淆

- 初始回答误认为变量和函数留在当前 Shell 是 `export` 的作用。
- 纠正后确认：它们留在当前 Shell 是因为 `source` 在当前 Shell 执行文件；`export` 负责让变量可继续传给子进程。

### source 是否需要执行权限

- 初始回答误认为 `source` 失败时应检查执行权限。
- 纠正后确认：`source` 读取文件，不要求文件具有执行权限；执行权限只是直接运行脚本时的要求。

### 不信任文件的影响

- 初始回答只笼统地认为可能导致系统崩溃。
- 纠正后确认：不可信文件中的命令会直接在当前 Shell 执行，可能修改变量、`PATH`、当前目录或文件，因此不应随意 `source`。

## 5. Day11 完成结论

- 所有直接运行、`source` 加载、失败排查、项目环境加载和清理操作均符合预期。
- 已能区分子 Shell 与当前 Shell 中的改动范围。
- 已理解 `source`、`export` 和执行权限各自的作用边界。
- Day11 所有核心学习目标已通过检查。

# Day12 用户、组与权限学习记录

## 1. 学习目标

- 理解用户表示操作身份，组表示一组用户的集合。
- 能确认当前用户、主组和附加组。
- 能从权限字符串中区分文件类型、owner、group 和 other。
- 理解系统根据当前身份选择一组权限，不会把三组权限相加。
- 通过最小实验复现权限拒绝，并恢复权限完成回归验证。
- 能根据身份、路径、文件归属和权限证据排查访问失败。

## 2. 权限字符串重点结构

> **重点：权限字符串由“文件类型 + owner + group + other”组成。**

```text
- rw- --- ---
│  │   │   └─ other：既不是 owner、也不属于文件所属组的其他用户
│  │   └───── group：属于文件所属组的用户
│  └───────── owner：文件所有者
└──────────── 文件类型：“-”是普通文件，“d”是目录
```

### 每个符号的含义

- `r`：允许读取。
- `w`：允许写入。
- `x`：允许执行；对目录表示允许通过该目录访问路径中的对象。
- `-`：对应位置没有该项权限。
- `---`：这一类用户没有读、写、执行权限，不是拥有全部权限。

### 系统选择哪组权限

```text
当前用户是文件 owner？
├─ 是 → 只检查 owner 权限
└─ 否 → 当前用户所属组与文件 group 匹配？
         ├─ 是 → 只检查 group 权限
         └─ 否 → 只检查 other 权限
```

- owner、group、other 是三种身份分类，不是三组可以相加的权限。
- 当前用户即使属于多个组，只要他就是文件 owner，本次访问就使用 owner 权限。
- `group` 指文件自身记录的所属组，不是“owner 所属的所有组”。
- `other` 指剩余用户，不是“owner 所属的其他用户组”。

## 3. 实际执行与结果

### 当前身份

- `whoami` 显示当前用户为 `seeway`。
- `id` 显示主组为 `l`，并显示当前用户的附加组。
- `groups` 显示 `l adm cdrom sudo dip plugdev lpadmin lxd sambashare docker`。
- 已能区分当前用户、主组和附加组，不要求记忆 UID、GID 数字。

### 临时权限对象

- 在 `/tmp/day12-permission-practice` 创建测试目录和 `status.txt`。
- 目录显示为 `drwx------`，owner 为 `seeway`，group 为 `l`。
- 文件显示为 `-rw-------`，owner 为 `seeway`，group 为 `l`。
- 目录的 owner 权限是 `rwx`；文件的 owner 权限是 `rw-`；group 和 other 均为 `---`。
- 测试对象位于 `/tmp`，不提交到 Git。

### 正常、异常与恢复验证

1. 当前用户 `seeway` 与文件 owner 匹配，owner 权限中存在 `r`，因此 `cat` 成功显示 `robot status: ready`。
2. 只移除 owner 的 `r` 后，文件变为 `--w-------`，`cat` 明确提示“权限不够”。
3. 恢复 owner 的 `r` 后，文件重新变为 `-rw-------`，`cat` 再次成功读取。
4. 实验保持当前用户和文件不变，只改变读权限，因此能够确认失败原因是 owner 缺少 `r`。

### 项目文件检查

- `day4_system_check.sh`、`day5_find_logs.sh`、`day6_log_analyzer.sh` 和 `day8_diagnostic_pipeline.sh` 均为 `-rwxr-xr-x`。
- 这些脚本的 owner 是 `seeway`，且 owner 权限中有 `x`，因此当前用户可以直接执行。
- `robot_env.sh` 为 `-rw-r--r--`，当前用户拥有读取权限。
- `source robot_env.sh` 是读取文件并在当前 Shell 执行内容，不要求文件本身具有执行权限。
- Day12 只读检查项目文件，没有修改它们的权限或归属。

## 4. 遇到的问题与纠正

### group 与 groups 命令混淆

- 现象：输入 `group` 后提示命令不存在。
- 处理：根据终端提示改用 `groups`，成功显示当前用户所属组。
- 结论：检查当前用户所属组使用 `groups` 或 `id`。

### group 和 other 的对象理解错误

- 初始理解：把 group 说成 owner 所属组，把 other 说成 owner 所属的其他组。
- 纠正：权限字符串中的 group 对应文件记录的所属组；other 对应既不是文件 owner、也不属于文件所属组的其他用户。
- 结论：先比较当前身份与文件的 owner、group，再决定使用哪一组权限。

### 文件存在为什么仍然不能读取

- 文件存在只证明路径中有这个对象，不代表当前身份拥有读取权限。
- `cat` 读取普通文件需要匹配权限组中的 `r`，同时路径中的目录也必须允许通过。
- 本次实验中 owner 的 `r` 被移除后，文件仍然存在，但当前用户无法读取内容。

### 权限问题的诊断顺序

1. 使用 `whoami`、`id` 或 `groups` 确认当前用户和所属组。
2. 确认路径及目标文件存在。
3. 检查路径目录和目标文件的 owner、group 与权限。
4. 判断当前身份匹配 owner、group 还是 other。
5. 检查匹配到的权限组是否允许所需操作。
6. 明确根因后做最小修改，再执行回归验证。

## 5. Day12 完成结论

- 已掌握用户、组、owner、group 和 other 的整体关系。
- 已能正确阅读权限字符串，并明确 `---` 表示没有权限。
- 已理解权限组按身份匹配，不会相加或自动降级到另一组。
- 已完成正常读取、权限拒绝和恢复后的回归验证。
- 已能按身份、路径、归属、权限和回归验证的顺序排查问题。
- Day12 所有核心学习目标已通过检查。

# Day13 chmod 与 chown 学习记录

## 1. 学习目标

- 区分 `chmod` 与 `chown` 修改的文件元数据。
- 通过变更前后的证据确认权限、owner 和 group 的变化范围。
- 理解普通用户改变自己文件所属组的边界。
- 完成合法 group 变更、非法 group 变更和恢复验证。
- 验证元数据变化不会修改文件正文或项目原文件。

## 2. 核心区别

| 命令 | 修改对象 | 不直接修改 |
|---|---|---|
| `chmod` | 文件权限 | owner、group、文件正文 |
| `chown` | owner、group，或同时修改两者 | 文件正文 |

- 权限、owner 和 group 都是文件元数据，不是文件正文。
- `chmod` 成功后要检查权限是否符合预期，同时确认 owner 和 group 未被意外改变。
- `chown` 成功后要检查 owner/group 是否符合预期，同时确认权限和内容未被意外改变。

## 3. 实际执行与结果

### 项目文件副本

- 将项目中的 `robot_env.sh` 复制为 `/tmp/day13-ownership-practice/robot_env.test.sh`。
- 项目原文件为 `-rw-r--r--`，owner 是 `seeway`，group 是 `l`。
- 临时副本初始为 `-rw-r-----`，owner 是 `seeway`，group 是 `l`。
- 后续实验只操作 `/tmp` 副本，没有修改项目原文件。

### chmod 权限变更

```text
变更前：-rw-r----- | owner=seeway | group=l
增加组写权限：-rw-rw---- | owner=seeway | group=l
恢复后：-rw-r----- | owner=seeway | group=l
```

- `chmod g+w` 只增加 group 的写权限。
- `chmod g-w` 成功恢复初始权限。
- owner 和 group 在整个过程中没有变化，证明本次 `chmod` 只修改权限。

### chown 合法变更

- 当前用户 `seeway` 的所属组中包含 `adm`。
- `chown :adm` 成功将临时副本的 group 从 `l` 改为 `adm`。
- 变更后 owner 仍为 `seeway`，权限仍为 `-rw-r-----`。
- 当前用户仍是文件 owner，因此访问时继续使用 owner 权限，不会改用 group 权限。

### chown 失败验证

- 当前用户不属于 `root` 组。
- `chown :root` 明确提示“不允许的操作”。
- 失败后 owner 仍为 `seeway`，group 仍为 `adm`，权限仍为 `-rw-r-----`。
- 已理解命令失败后还要检查当前状态，不能只记录错误文字。

### 恢复与回归验证

- 使用 `chown :l` 将副本 group 恢复为 `l`。
- `cmp` 显示 `[OK] file content unchanged`，证明元数据操作没有修改文件正文。
- 项目原文件最终仍为 `-rw-r--r--`、owner `seeway`、group `l`。
- Git 对项目 `robot_env.sh` 没有显示修改。
- `/tmp/day13-ownership-practice` 不作为 Git 提交内容。

## 4. 遇到的问题与纠正

### chown 的职责范围

- 初始回答只认为 `chown` 修改 group。
- 纠正：`chown` 可以修改 owner、group，或者同时修改两者；本次实验只选择修改 group。

### adm 与 root 的含义

- 初始回答把成功和失败归因于“普通用户”和“高级管理员用户”。
- 纠正：`:adm` 与 `:root` 在本次命令中指定的都是组名，不是当前执行命令的用户身份。
- 成功原因是当前用户属于 `adm`；失败原因是当前用户不属于 `root` 组，普通用户不能把自己的文件任意改为非所属组。

### chmod/chown 是否修改文件内容

- 初始回答把两个命令都说成只修改权限。
- 纠正：`chmod` 修改权限，`chown` 修改 owner/group；二者都不修改文件正文。

### 操作不允许时的检查顺序

1. 检查当前用户和所属组。
2. 检查文件当前的 owner、group 和权限。
3. 确认请求修改的目标 owner/group。
4. 判断当前用户是否有权执行该变更。
5. 失败后使用 `stat` 确认文件状态没有发生意外变化。

### 不能只用 ls 完成回归验证

- `ls -l` 可以查看权限、owner 和 group，但不能证明文件正文没有变化。
- 完整验证需要比较变更前后的 `stat`，使用 `cmp` 检查正文，并用 Git 状态确认项目原文件没有变化。

## 5. Day13 完成结论

- 已正确区分 `chmod` 和 `chown` 的职责。
- 已通过实际输出证明 `chmod` 只改变目标权限。
- 已完成允许和拒绝两条 `chown` 路径，并检查失败后的状态。
- 已恢复临时副本的权限和 group。
- 已确认文件正文和项目原文件均未被修改。
- Day13 所有核心学习目标已通过检查。

# Day14 进程、PID、ps 与 top 学习记录

## 1. 学习目标

- 区分磁盘上的程序文件与运行中的进程实例。
- 理解 PID 和 PPID 的作用及父子进程关系。
- 使用 `ps` 获取进程快照，使用 `top` 观察目标进程和系统概要。
- 通过临时 worker 验证进程从出现、运行到自然结束的生命周期。
- 如实检查当前系统中的 ROS2 相关进程。

## 2. 核心概念

### 程序与进程

- 程序是存储在磁盘上的代码文件，没有运行时也可以存在。
- 进程是程序运行后产生的实例，具有 PID、PPID、运行用户、状态和资源。
- 进程结束后该运行实例消失，程序文件通常仍然存在。

### PID 与 PPID

- PID 标识当前正在运行的某个进程实例。
- PPID 表示启动该进程的父进程 PID。
- PID 在当前时刻可用于精确定位实例，但不是永久编号；进程结束后，该数字可能被系统重新使用。
- 因此旧 PID 不能永久充当进程仍在运行的证据，必须重新查询。

### ps 与 top

- `ps` 获取执行命令那个时刻的进程快照。
- `top` 正常交互使用时持续刷新系统和进程状态。
- Day14 使用 `top` 的单次批处理输出，方便在 `out.md` 保存证据。
- `htop` 未安装不影响进程观察，Day14 没有为了练习安装软件。

## 3. 实际执行与结果

### 当前 Shell 和系统快照

- 当前 Shell PID 为 `4994`，`ps` 输出中的 PID 同样为 `4994`。
- 当前 Shell PPID 为 `4880`，运行用户为 `seeway`。
- 系统进程快照中能正确指出 PID、PPID、USER、状态、运行时间和命令名。

### 临时诊断 worker

- 当前 Shell 启动了名为 `robot-system-diagnostic-worker` 的临时 worker。
- worker PID 为 `95307`，PPID 为 `4994`。
- worker PPID 与当前 Shell PID 一致，证明当前 Shell 是它的父进程。
- `ps`、`pgrep` 和 `top` 都在 worker 存活期间显示 PID `95307`。
- `top` 显示该进程当时处于 sleeping 状态，命令为 `sleep`。

### 自然结束验证

- worker 在8秒后自然结束，没有发送终止信号。
- 最终 `ps` 查询只显示表头，不再显示 PID `95307`。
- 终端输出 `[EXPECTED] worker is no longer running`，证明该 worker 已结束。
- 仅打印 `$!` 得到 PID 不能单独证明进程仍在运行；必须由 `ps`、`pgrep` 或 `top` 的实际进程行确认。

### htop 与 ROS2 检查

- `command -v htop` 确认系统未安装 `htop`，继续使用 `ps/top`。
- 当前进程快照没有匹配到名称中包含 `ros2` 或 `robot_state_publisher` 的进程。
- 无匹配只表示本次快照没有获得相应运行证据，不能证明 ROS2 软件没有安装，也不能代表系统以后不会运行 ROS2 节点。

## 4. 遇到的问题与纠正

### 程序文件不是永久不变

- 初始回答把程序文件描述为“永久写在磁盘上”。
- 纠正：程序文件可以被修改或删除；关键区别是它独立于某次运行实例存在。

### PID 不是永久唯一

- 初始回答只说 PID 唯一。
- 纠正：PID 在进程存活和当前观察时刻用于定位实例；进程结束后，PID 数字可能被其他新进程复用。
- 使用已知 PID 比名称搜索精确，是因为名称可能同时匹配多个进程，但仍需重新查询确认该 PID 当前对应什么。

### 证明进程运行需要实际查询

- 初始回答认为输出 worker PID 就能证明它运行过。
- 纠正：Shell 分配并打印 PID 只是启动线索；`ps`、`pgrep` 和 `top` 显示对应进程行才是它正在运行的证据。
- `wait` 后再次查询不再出现该 PID，才形成进程已经结束的回归证据。

## 5. 基础诊断顺序

1. 明确预期运行的程序或命令名称。
2. 获取当前进程快照并搜索目标。
3. 有结果时记录 PID、PPID、USER、STAT、ETIME 和 CMD。
4. 对已知 PID 做精确查询，确认当前对应实例。
5. 无结果时区分进程已结束、搜索名称不准确和程序未启动，不能直接认定软件未安装。
6. 需要持续观察时重新获取当前证据。

## 6. Day14 完成结论

- 已理解程序文件、进程实例、PID 和 PPID 的整体关系。
- 已使用 `ps` 和 `top` 获取同一目标进程的运行证据。
- 已完成临时 worker 出现、运行和自然结束的生命周期验证。
- 已理解 PID 可能被复用，旧 PID 不能永久代表原进程。
- 已如实记录 `htop` 与 ROS2 相关进程检查结果。
- Day14 所有核心学习目标已通过检查。

# Day15 kill、pkill 与 signal 学习记录

## 1. 学习目标

- 理解 signal 是发送给进程的通知，`kill` 的核心作用是发送信号。
- 理解 `TERM` 用于请求进程结束，目标进程可以在退出前执行清理。
- 在发送信号前确认目标，发送后重新查询结果。
- 区分 `kill` 按 PID 定位与 `pkill` 按名称或模式匹配。
- 使用 `process_check.sh` 检查指定 PID 是否正在运行或可访问。

## 2. 核心概念

### kill、TERM 与进程退出

- `kill` 并不等于无条件强制结束进程，它的核心动作是向目标进程发送信号。
- Day15 使用 `TERM` 请求临时 worker 正常结束，没有实践强制终止信号。
- 目标进程可以处理 `TERM`，先执行清理逻辑，再退出。
- 发送命令成功不能单独证明进程已经退出，必须重新查询目标状态。

### kill 与 pkill 的目标范围

- `kill` 根据 PID 定位一个明确的进程实例。
- `pkill` 根据进程名称或模式选择目标，一次可能匹配多个进程。
- `pkill` 不是固定意义上的模糊搜索；匹配范围取决于名称、模式和使用方式。
- 使用 `pkill` 前，应先用完全相同的精确模式查看匹配结果，确认不会影响无关进程。

### kill -0

- `kill -0` 不发送终止信号，只检查目标进程是否存在且当前用户是否可以访问。
- 检查成功表示目标存在并且当前用户有相应访问权限。
- 检查失败可能表示进程不存在，也可能表示当前用户无权访问，不能只解释为进程不存在。

## 3. 实际执行与结果

### 按 PID 发送 TERM

- 在 `/tmp/day15-signal-practice` 中创建了能处理 `TERM` 的临时 worker。
- worker PID 为 `101498`，`ps` 和 ready 日志共同证明它正在运行。
- 向已确认的 PID 发送 `TERM` 后，日志显示 `[WORKER] TERM received; cleanup complete`。
- 清理日志证明 worker 收到 `TERM` 并执行了清理；随后 `ps` 查不到该 PID，证明它已经退出。
- 临时 worker 和日志位于 `/tmp`，不提交到 Git。

### 不存在 PID 的安全验证

- 先使用 `ps` 确认测试 PID `99999999` 不存在。
- 随后发送 `TERM`，终端明确提示“没有那个进程”。
- 异常测试没有使用旧 PID、未知 PID 或系统进程，避免了 PID 复用和误操作风险。

### pkill 名称匹配验证

- 创建了两个命令行名称为 `day15-pkill-worker` 的临时进程，PID 分别为 `102017` 和 `102018`。
- 发送信号前，精确模式只匹配这两个临时进程。
- 使用相同模式执行 `pkill -TERM` 后，再次查询没有匹配结果。
- 实验说明同名进程可以有不同 PID，而名称或模式匹配可能一次影响多个实例。

### process_check.sh 项目实践

- 新增 `robot-system-learning/linux/process_check.sh`。
- 脚本先检查 PID 是否为空或格式无效，再使用 `kill -0` 检查目标。
- 目标可访问时输出 `[PROCESS RUNNING]` 和对应的 `ps` 证据。
- 目标不存在或不可访问时输出 `[PROCESS NOT RUNNING OR NOT ACCESSIBLE]`。
- 脚本通过 `bash -n` 语法检查，并具有 owner 执行权限。
- 对 PID `102608` 的测试先得到运行中结果，发送 `TERM` 并等待后得到不再运行或不可访问结果。

## 4. 遇到的问题与纠正

### 清理证据与退出证据不能混为一项

- 初始回答认为最终 `ps` 查不到 PID 可以证明 worker 执行了清理。
- 纠正：`[WORKER] TERM received; cleanup complete` 日志证明收到信号并执行清理；最终 `ps` 无目标行只证明进程已经退出。
- 两项证据组合起来，才能说明 worker 收到 `TERM`、完成清理并最终退出。

### pkill 不是简单的模糊搜索

- 初始回答把 `pkill` 描述为模糊搜索。
- 纠正：`pkill` 按名称或模式匹配，本次使用 `-f` 和带开头限制的模式匹配完整命令行。
- 模式越宽，影响无关进程的风险越大，因此必须先查看相同模式的匹配范围。

### 信号操作的安全顺序

1. 只选择本次创建且身份明确的临时进程。
2. 使用 PID、用户、命令行或精确名称模式确认目标。
3. 确认匹配范围后发送 `TERM`。
4. 查看目标程序的清理日志。
5. 重新查询 PID 或名称，确认最终状态。

## 5. Day15 完成结论

- 已理解 signal、`kill` 和 `TERM` 的整体关系。
- 已完成按 PID 正常结束、不存在 PID 和 `pkill` 批量匹配三条验证路径。
- 已能区分清理日志与进程退出查询各自证明的事实。
- 已理解按名称或模式匹配的范围风险，并能在操作前确认目标。
- `process_check.sh` 已完成，能够区分运行中与不存在或不可访问的 PID。
- 所有信号只发送给本次创建的临时进程或已确认不存在的安全测试 PID。
- Day15 所有核心学习目标已通过检查。
